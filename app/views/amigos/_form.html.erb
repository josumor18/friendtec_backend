<%= form_for(amigo) do |f| %>
  <% if amigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(amigo.errors.count, "error") %> prohibited this amigo from being saved:</h2>

      <ul>
      <% amigo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_user1 %>
    <%= f.number_field :id_user1 %>
  </div>

  <div class="field">
    <%= f.label :id_user2 %>
    <%= f.number_field :id_user2 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
